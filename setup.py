from setuptools import setup,find_packages

setup(
        name = "src" , 
        version = "0.0.1",
        description = "Its a Mlops package",
        author = "Rituparna",
        packages = find_packages(),
        licences = "MIT"

)